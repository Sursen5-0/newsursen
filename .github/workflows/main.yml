name: .NET Core Full Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_call:

permissions:
  checks: write
  pull-requests: write

jobs:
  build_backend:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install .NET Core
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Restore Dependencies
      run:  dotnet restore 

    - name: Build Solution
      run: dotnet build --configuration Release
      
    - name: Set doppler_key as machine variable
      run: |
        [System.Environment]::SetEnvironmentVariable("doppler_key", "${{ secrets.DOPPLER_KEY }}", "Machine")
      shell: pwsh

    - name: Set env as machine variable
      run: |
        [System.Environment]::SetEnvironmentVariable("Environment", "${{ secrets.Env }}", "Machine")
      shell: pwsh
    
    - name: Build Docker Image
      run: |
        docker build . -f Backend/Hangfire/Dockerfile -t bjand49/sursen-jobs:latest

    - name: Set doppler_key as machine variable
      run: |
        [System.Environment]::SetEnvironmentVariable("doppler_key", "${{ secrets.DOPPLER_KEY }}", "Machine")
      shell: pwsh

    - name: Set env as machine variable
      run: |
        [System.Environment]::SetEnvironmentVariable("Environment", "${{ secrets.Env }}", "Machine")
      shell: pwsh

    - name: Run Unit Tests
      run: dotnet test --configuration Release --no-build --verbosity normal
      
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: bjand49
        password: ${{ secrets.DOCKER_PAT }}

    - name: Push Docker Image
      run: docker push bjand49/sursen-jobs
